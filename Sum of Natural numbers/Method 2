#include <stdio.h>
int main()
{
    int num,i=1,sum=0;
    printf ("Enter n term: ");
    scanf ("%d",&num);
    while (i<=num)
    {
        sum+=i;
        i++;
    }
    Printf ("Sum of %d natural numbers: %d\n",num,sum);
    return 0;
}
