#include <stdio.h>
int sum (int n)
{
    if (n==1 || n==0)
        return n;
    return n+sum(n-1);
}
int main()
{
    int n;
    printf("Enter n term of natural number: ");
    scanf("%d",&n);
    printf("Sum of %d natural numbers is: %d\n",n,sum(n));
    return 0;
}
