#include <stdio.h>
int main()
{
    int n,num,i,sum=0;
    float avg;
    printf ("Enter limit of numbers: ");
    scanf ("%d",&n);
    printf ("Enter %d numbers: ",n);
    for (i=0; i<n; i++)
    {
        scanf ("%d",&num);
        sum+=num;
    }
    printf ("Sum of given %d numbers: %d\n",n,sum);
    avg=(float)sum/n;
    printf ("Average of given numbers: %.2f\n",avg);
    return 0;
}
