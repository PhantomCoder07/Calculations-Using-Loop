#include <stdio.h>
int main()
{
    int i,num,sum=0;
    printf("Enter a integer number:");
    scanf("%d",&num);
    for (i=1; i<=num; i++)
    {
        sum+=2*i;
    }
    printf("Sum of first %d even number: %d\n",num,sum);
    return 0;
}
